# Лабораторная работа №1
## Работа с Git и GitHub

**Группа:** АБс-524

**Выполняли:**
- Добринов - треугольник
- Комаров - прямоугольник
- Сафонов - окружность

---

# Цель работы

Изучение платформы **GitHub** и практическое использование **Git** для хранения, обновления и распространения исходного кода проекта.

---

# Задание по варианту

Реализовать функции вычисления параметров различных **геометрических фигур**.

Каждый участник реализует одну фигуру.

| Студент | Фигура | Реализованные функции |
|--------|--------|--------|
| Сафонов | Окружность | длина окружности, площадь круга, площадь сектора |
| Добринов | Треугольник | периметр, площадь по формуле Герона, проверка на равнобедренность |
| Комаров | Прямоугольник | периметр, площадь, диагональ |

---

# Описание проекта

Программа написана на языке **C++** и позволяет пользователю выбрать геометрическую фигуру и выполнить вычисление её параметров.

После запуска программы пользователь выбирает фигуру из списка:
- 1 - Прямоугольник
- 2 - Треугольник
- 3 - Окружность

После этого вводятся необходимые данные, и программа выводит результат вычислений.

---

# Структура проекта

```
project/
│
├── main.cpp
├── enums.h
│
├── math.h
├── math.cpp
│
├── circle.h
├── circle.cpp
│
├── ractangle.h
├── ractangle.cpp
│
└── README.md
```

---

# Листинг программы

## main.cpp

```cpp
int main() {
    std::cout << "1-Прямоугольник" << std::endl
    << "2-Треугольник" << std::endl
    << "3-Окружность" << std::endl;

    int input;
    std::cin >> input;

    switch (input) {
        case figures::RECTANGLE:
            ractangle();
            break;
        case figures::TRIANGLE:
            math::testTriangle();
            break;
        case figures::CIRCLE:
            circle();
            break;
        default:
            std::cout << "Ошибка нужны цифры от 1 до 4" << std::endl;
    }
}
```

---

# Реализация треугольника (Добринов)
Функции:
- вычисление периметра 
- вычисление площади по формуле Герона 
- проверка на равнобедренность

```cpp
double Triangle::Perimeter() const {
return this->a + this->b + this->c;
}

double Triangle::Area() const {
double p = this->Perimeter() / 2.0;
return std::sqrt(p * (p - a) * (p - b) * (p - c));
}

bool Triangle::IsEqualSized() const {
return (a == b) || (a == c) || (b == c);
}
```


# Реализация прямоугольника (Комаров)
Функции:
- периметр
- площадь
- диагональ

```cpp
void ractangle(){
    double length = 0, width  = 0;


    std::cout << "Длина и ширина прямоугольника" << std::endl;
    if ((std::cin >> length && length > 0) &&
    (std::cin >> width && width > -1)) {
        std::cout << std::fixed << std::setprecision(2) << "Периметр = "
                  << 2 * (length + width) << std::endl;

        std::cout << std::fixed << std::setprecision(2) << "Площадь = "
                  << length * width << std::endl;

        std::cout << std::fixed << std::setprecision(2) << "Диагональ = "
                  <<sqrt(length * length + width * width) << std::endl;
    }
    else {
        std::cout << "Ошибка, сторона должна быть > 0" << std::endl;
    }
}
```


# Реализация окружности (Сафонов)
Функции:
- длина окружности
- площадь круга
- площадь кругового сектора

```cpp
void circle() {
    double radius = 0;
    int corner = 0;
    float pi = 3.14;

    std::cout << "Радиус окружности и угол в градусах для поиска площади сектора" << std::endl;

    if ((std::cin >> radius && radius > 0) &&
        (std::cin >> corner && corner > -1)) {

        std::cout << std::fixed << std::setprecision(2)
                  << "Длина окружности - "
                  << 2 * pi * radius << std::endl;

        std::cout << std::fixed << std::setprecision(2)
                  << "Площадь круга - "
                  << pi * radius * radius << std::endl;

        std::cout << std::fixed << std::setprecision(2)
                  << "Площадь кругового сектора - "
                  << pi * radius * radius * corner / 360 << std::endl;

    } else {
        std::cout << "Ошибка радиус больше 0 и угол от 0 градусов" << std::endl;
    }
}
```
---
# Пример работы программы

```
1-Прямоугольник
2-Треугольник
3-Окружность

2

Введите стороны: 3 4 5

Треугольник: a=3 b=4 c=5
Периметр: 12
Площадь (формула Герона): 6
Проверка на равнобедренность: 0
```
---
# Работа с Git и GitHub

## Создание репозитория
```
git init
```

## Клонирование проекта
```
git clone https://github.com/pro1oo100/Laba1
```

## Создание ветки
```
git checkout -b Alexander
```

## Добавление файлов
```
git add .
```

## Коммит изменений
```
git commit -m "Добавлена реализация треугольника"
```

## Отправка на GitHub
```
git push origin Alexander
```

## Слияние веток
```
git merge Alexander
```

## Получение изменений
```
git pull
```
---

# Итог
В ходе лабораторной работы:
- изучена система контроля версий Git 
- освоена платформа GitHub 
- реализована программа вычисления параметров геометрических фигур 
- выполнена работа с ветками, коммитами и слиянием 
- получен опыт совместной разработки проекта
